# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=st_intent_harvest_production

# Rails Configuration
SECRET_KEY_BASE=your_secret_key_base_here_minimum_64_characters_generate_with_rails_secret
RAILS_MAX_THREADS=5
WEB_CONCURRENCY=2

# Port Configuration - Choose an available port from your server
# Available ports from your server: 3001, 3002, 3003, 3004, 3005, etc.
# Avoid: 22 (SSH), 53 (DNS), 80 (HTTP), 3000 (other app), 4096 (PM2), 65529, 33060, 70
PORT=3005

# Disable/Enable SSL
RAILS_FORCE_SSL=false
RAILS_ASSUME_SSL=false