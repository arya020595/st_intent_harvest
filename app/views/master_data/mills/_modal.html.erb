<%# Mills Modal - Uses Shared Modal Component %>
<%= render "shared/modal", **modal_config(
      id: "millModal",
      default_size: "modal-lg",
      centered: true,
      frame_id: "modal"
    ) %>
