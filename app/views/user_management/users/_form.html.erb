<%= form_with(model: [:user_management, user]) do |form| %>
  <div class="modal-header">
    <h5 class="modal-title" id="userModalLabel">
      <%= user.persisted? ? 'Edit User' : 'Add New User' %>
    </h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
  </div>
  <div class="modal-body">
    <% if user.errors.any? %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <h6 class="alert-heading">
          <i class="bi bi-exclamation-triangle-fill"></i>
          <%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:
        </h6>
        <ul class="mb-0 mt-2">
          <% user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
      </div>
    <% end %>
    <div class="row">
      <div class="col-md-6 mb-3">
        <%= form.label :name, class: "form-label fw-semibold" %>
        <%= form.text_field :name, class: "form-control", placeholder: "Enter full name", required: true %>
      </div>
      <div class="col-md-6 mb-3">
        <%= form.label :email, class: "form-label fw-semibold" %>
        <%= form.email_field :email, class: "form-control", placeholder: "Enter email address", required: true %>
      </div>
    </div>
    <div class="mb-3">
      <%= form.label :role_id, "Role", class: "form-label fw-semibold" %>
      <%= form.collection_select :role_id, Role.all, :id, :name, { include_blank: "Select Role" }, { class: "form-select", required: true } %>
    </div>
    <% if user.new_record? %>
      <div class="row">
        <div class="col-md-6 mb-3">
          <%= form.label :password, class: "form-label fw-semibold" %>
          <%= form.password_field :password, class: "form-control", placeholder: "Enter password", required: true %>
          <small class="form-text text-muted">Minimum 6 characters</small>
        </div>
        <div class="col-md-6 mb-3">
          <%= form.label :password_confirmation, "Confirm Password", class: "form-label fw-semibold" %>
          <%= form.password_field :password_confirmation, class: "form-control", placeholder: "Re-enter password", required: true %>
        </div>
      </div>
    <% end %>
    <div class="mb-3">
      <div class="form-check form-switch">
        <%= form.check_box :is_active, class: "form-check-input", role: "switch" %>
        <%= form.label :is_active, "Active Status", class: "form-check-label" %>
        <small class="form-text text-muted d-block">Toggle to set user as active or inactive</small>
      </div>
    </div>
  </div>
  <div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
      <i class="bi bi-x-circle"></i> Cancel
    </button>
    <% if user.persisted? %>
      <%= form.submit 'Update User', class: "btn btn-primary" %>
    <% else %>
      <%= form.submit 'Save User', class: "btn btn-primary" %>
    <% end %>
  </div>
<% end %>
