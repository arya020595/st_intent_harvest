<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h1>Pay Calculation Details</h1>
      <div class="card">
        <div class="card-body">
          <dl class="row">
            <dt class="col-sm-3">Month/Year:</dt>
            <dd class="col-sm-9"><%= @pay_calculation.month_year %></dd>
            <dt class="col-sm-3">Overall Total:</dt>
            <dd class="col-sm-9"><%= @pay_calculation.overall_total %></dd>
          </dl>
          <h3 class="mt-4">Worker Details</h3>
          <div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>Worker Name</th>
                  <th>Gross Salary</th>
                  <th>Deductions</th>
                  <th>Net Salary</th>
                </tr>
              </thead>
              <tbody>
                <% @pay_calculation.pay_calculation_details.each do |detail| %>
                  <tr>
                    <td><%= detail.worker.name %></td>
                    <td><%= detail.gross_salary %></td>
                    <td><%= detail.deductions %></td>
                    <td><%= detail.net_salary %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <div class="mt-3">
            <%= link_to "Edit", edit_work_order_pay_calculation_path(@pay_calculation), class: "btn btn-warning" %>
            <%= link_to "Back", work_order_pay_calculations_path, class: "btn btn-secondary" %>
            <%= link_to "Delete", work_order_pay_calculation_path(@pay_calculation), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
